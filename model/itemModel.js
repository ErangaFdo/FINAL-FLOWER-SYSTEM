export default class itemModel {
    constructor(itemId,itemName,qty,color,price) {
        this.itemId = itemId
        this.itemName = itemName
        this.qty = qty
        this.color = color
        this.price = price
    }
}